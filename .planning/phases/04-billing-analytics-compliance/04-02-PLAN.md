# Plan: 04-02 - GDPR Webhooks & Compliance

**Wave**: 2
**Depends on**: 04-01-PLAN.md
**Files modified**: shopify.app.toml, app/routes/webhooks.tsx
**Autonomous**: true

## Goal
Implement the mandatory GDPR webhooks required for Shopify App Store approval.

## Tasks

<task>
<read_first>
- shopify.app.toml
</read_first>
<action>
Register the three GDPR webhook topics:
- `customers/data_request` -> `/webhooks/gdpr/data_request`
- `customers/redact` -> `/webhooks/gdpr/customers_redact`
- `shop/redact` -> `/webhooks/gdpr/shop_redact`
</action>
<acceptance_criteria>
- GDPR topics are registered in the TOML file.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/webhooks.tsx (or new routes)
</read_first>
<action>
Implement the handlers for these webhooks.
For MVP, these can just log the request and return a 200 OK (since we don't store much customer data yet).
</action>
<acceptance_criteria>
- Endpoints return 200 OK for valid Shopify webhook requests.
</acceptance_criteria>
</task>

## Verification
- Use `shopify app webhook trigger` to test each GDPR topic.
- Verify that your server logs the request and returns a 200 status.

## Must Haves
- All three GDPR webhooks implemented and returning 200 OK.
