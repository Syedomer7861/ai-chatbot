# Plan: 02-02 - Product & Policy Sync

**Wave**: 2
**Depends on**: 02-01-PLAN.md
**Files modified**: prisma/schema.prisma, app/shopify.server.ts, app/routes/webhooks.tsx
**Autonomous**: true

## Goal
Implement the data model and background sync logic for products and store policies.

## Tasks

<task>
<read_first>
- prisma/schema.prisma
</read_first>
<action>
Update the Prisma schema to include `KnowledgeItem`.
Add fields: `shopId`, `type` (ENUM: PRODUCT, POLICY), `sourceId`, `content`, `metadata`.
Run `npx prisma generate` and `npx prisma migrate dev`.
</action>
<acceptance_criteria>
- `prisma/schema.prisma` contains the `KnowledgeItem` model.
- Migration is successful.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/shopify.server.ts
</read_first>
<action>
Register webhooks in `shopify.server.ts` or `shopify.app.toml`:
- `PRODUCTS_CREATE`
- `PRODUCTS_UPDATE`
- `PRODUCTS_DELETE`
Configure the `afterAuth` hook to trigger an initial "Full Sync" of active products.
</action>
<acceptance_criteria>
- Webhook registration logic is present.
- `afterAuth` contains a call to a (to be implemented) sync function.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/webhooks.app.products.tsx (new)
</read_first>
<action>
Implement the product webhook handlers to update/upsert `KnowledgeItem` in the database.
Map the product body (title, body_html, price) to the `content` field.
</action>
<acceptance_criteria>
- Webhook route correctly handles `POST` requests and updates the database.
</acceptance_criteria>
</task>

## Verification
- Use the Shopify Admin to update a product and check the database (e.g., `npx prisma studio`) for a matching `KnowledgeItem`.
- Verify the `afterAuth` trigger correctly fetches active products.

## Must Haves
- KnowledgeItem model in database.
- Real-time product sync via webhooks.
- Initial sync on app install.
