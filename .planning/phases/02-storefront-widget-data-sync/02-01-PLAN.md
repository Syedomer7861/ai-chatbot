# Plan: 02-01 - Theme App Extension & Shadow DOM Widget

**Wave**: 1
**Depends on**: Phase 1
**Files modified**: extensions/ai-chatbot-widget/*
**Autonomous**: true

## Goal
Generate the Theme App Extension and implement a modern, Shadow-DOM isolated chat bubble.

## Tasks

<task>
<read_first>
- .planning/phases/02-storefront-widget-data-sync/01-RESEARCH.md
</read_first>
<action>
Generate the theme extension:
`npm run shopify app generate extension -- --type theme_app_extension --name ai-chatbot-widget`
</action>
<acceptance_criteria>
- `extensions/ai-chatbot-widget` directory exists.
- `blocks/chatbot.liquid` exists.
</acceptance_criteria>
</task>

<task>
<read_first>
- extensions/ai-chatbot-widget/blocks/chatbot.liquid
</read_first>
<action>
Implement the `chatbot.liquid` block with a custom element and schema:
- Add a floating bubble container.
- Define a schema with "Primary Color" and "Bot Name" settings.
</action>
<acceptance_criteria>
- `chatbot.liquid` contains the schema with at least one color setting.
- `chatbot.liquid` renders a `<ai-chatbot-widget>` custom element.
</acceptance_criteria>
</task>

<task>
<read_first>
- extensions/ai-chatbot-widget/assets/chatbot.js
</read_first>
<action>
Implement the `ChatbotWidget` custom element using Shadow DOM (Open mode).
Add basic styles for a floating bubble in `assets/chatbot.css` and link it in the shadow root.
</action>
<acceptance_criteria>
- `assets/chatbot.js` defines a `customElements.define('ai-chatbot-widget', ...)` class.
- The widget renders a "Chat with us" button that toggles a simple modal.
</acceptance_criteria>
</task>

## Verification
- Run `npm run dev` and verify that the extension appears in the Shopify Theme Editor (App Blocks).
- Inspect the storefront and verify the widget is isolated within a `#shadow-root`.

## Must Haves
- Theme app extension generated.
- Shadow DOM isolation for styles.
- Configurable colors via Liquid schema.
