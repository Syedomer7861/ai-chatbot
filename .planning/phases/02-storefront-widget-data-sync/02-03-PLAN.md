# Plan: 02-03 - Knowledge Ingestion & Search Foundation

**Wave**: 3
**Depends on**: 02-02-PLAN.md
**Files modified**: app/models/knowledge.server.ts, app/routes/api.chat.ts
**Autonomous**: true

## Goal
Build the retrieval foundation: process store policies and implement a basic search API for the chatbot.

## Tasks

<task>
<read_first>
- app/shopify.server.ts
</read_first>
<action>
Implement a policy sync function that fetches Refund, Shipping, and Privacy policies from the Shopify Admin API.
Store them as `KnowledgeItem` with `type: POLICY`.
</action>
<acceptance_criteria>
- Store policies are persisted in the database.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/models/knowledge.server.ts (new)
</read_first>
<action>
Implement a `searchKnowledge` function that performs a simple keyword/semantic-light search on the `KnowledgeItem` table.
For MVP, use Prisma `findMany` with `contains` or `fts` (if available in SQLite).
</action>
<acceptance_criteria>
- `searchKnowledge` returns relevant product/policy snippets for a given query.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/api.chat.ts (new)
</read_first>
<action>
Create a public (proxy-authenticated) chat API endpoint.
- Accepts a `message` and `shop`.
- Retrieves relevant context using `searchKnowledge`.
- Returns the context to the client (AI generation comes in Phase 3).
</action>
<acceptance_criteria>
- `/api/chat` returns JSON with matching knowledge snippets.
</acceptance_criteria>
</task>

## Verification
- Test the `/api/chat` endpoint with a query like "Return policy" and verify it returns the policy text.
- Verify product results are returned for product-specific queries.

## Must Haves
- Store policies synced.
- Basic retrieval API working.
