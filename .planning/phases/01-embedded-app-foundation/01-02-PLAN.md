# Plan: 01-02 - Shopify OAuth & App Bridge v4

**Wave**: 2
**Depends on**: 01-01-PLAN.md
**Files modified**: .env, app/routes/auth.$, app/shopify.server.ts
**Autonomous**: true

## Goal
Implement the Shopify OAuth flow and ensure the app correctly handles session tokens via App Bridge v4.

## Tasks

<task>
<read_first>
- .env.example (if exists)
- .planning/phases/01-embedded-app-foundation/01-CONTEXT.md
</read_first>
<action>
Initialize `.env` file with placeholders for Shopify secrets:
```env
SHOPIFY_API_KEY=your_api_key
SHOPIFY_API_SECRET=your_api_secret
SCOPES=read_products,read_content
SHOPIFY_APP_URL=https://your-app-url.com
```
</action>
<acceptance_criteria>
- `.env` exists with the four required keys.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/shopify.server.ts
</read_first>
<action>
Configure the Shopify App object in `app/shopify.server.ts` to use Prisma and the latest API version (2025-04 or latest stable).
Ensure `distribution: AppDistribution.AppStore` is set if applicable.
</action>
<acceptance_criteria>
- `shopify.server.ts` imports `@shopify/shopify-app-remix` (or React Router equivalent).
- Prisma adapter is initialized with the Shopify object.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/_index.tsx
</read_first>
<action>
Implement a basic "Dashboard" landing page using `<polaris-page>` and `<polaris-layout>`.
Include a simple "Welcome" message to verify Polaris components are rendering.
</action>
<acceptance_criteria>
- `app/routes/_index.tsx` uses Polaris Web Components (e.g., `<polaris-text variant="headingLg">Welcome</polaris-text>`).
</acceptance_criteria>
</task>

## Verification
- Run `npm run dev` and verify that the local server starts without environment errors.
- Verify that the `auth` routes are generated by the template.

## Must Haves
- .env file configured.
- Shopify server object initialized with Prisma.
- Dashboard route using Polaris Web Components.
