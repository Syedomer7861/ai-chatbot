# Plan: 03-02 - Admin Dashboard UI

**Wave**: 2
**Depends on**: 03-01-PLAN.md
**Files modified**: prisma/schema.prisma, app/routes/app._index.tsx
**Autonomous**: true

## Goal
Build the merchant's control panel using Polaris components for settings and analytics.

## Tasks

<task>
<read_first>
- prisma/schema.prisma
</read_first>
<action>
Add `AppSettings` model to Prisma:
- `shop` (Unique)
- `botName` (Default: "AI Assistant")
- `botPersona` (Text)
Run migration.
</action>
<acceptance_criteria>
- Database schema updated.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/app._index.tsx
</read_first>
<action>
Implement the dashboard layout:
- **Card 1 (Stats)**: Display total KnowledgeItems and a dummy "Conversations" count.
- **Card 2 (Settings)**: Form with "Bot Name" and "System Instructions" text fields.
- **Save Action**: Implement the React Router `action` to persist settings.
</action>
<acceptance_criteria>
- Dashboard shows current knowledge base count.
- Settings form successfully updates the database.
</acceptance_criteria>
</task>

## Verification
- Change the "Bot Name" in Admin and verify it reflects in the storefront widget (via attribute or API).
- Check the stats card for accurate counts.

## Must Haves
- Settings persistence in database.
- Polaris-based dashboard UI.
- Knowledge base stats visible to merchant.
