# Plan: 03-03 - Manual Sync & Progress UI

**Wave**: 3
**Depends on**: 03-02-PLAN.md
**Files modified**: app/routes/app._index.tsx, app/routes/api.sync.ts
**Autonomous**: true

## Goal
Give the merchant a way to manually trigger a data re-sync and see progress.

## Tasks

<task>
<read_first>
- app/models/knowledge.server.ts
</read_first>
<action>
Create `app/routes/api.sync.ts` (Admin-authenticated) that:
1. Calls `syncPolicies`.
2. Triggers a full product re-fetch.
3. Updates a "Last Synced" timestamp in `AppSettings`.
</action>
<acceptance_criteria>
- `/api/sync` successfully updates the knowledge base.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/app._index.tsx
</read_first>
<action>
Add a "Sync Now" button to the Dashboard stats card.
Implement a `loading` state for the button using `useFetcher`.
Show a success toast when sync finishes.
</action>
<acceptance_criteria>
- Merchant can trigger sync from the UI.
- UI provides visual feedback (spinner/toast).
</acceptance_criteria>
</task>

## Verification
- Click "Sync Now" and verify that `KnowledgeItem` counts are updated (or last synced time changes).
- Ensure the button is disabled while syncing.

## Must Haves
- Manual sync endpoint.
- Sync button with visual feedback.
- Success notifications.
