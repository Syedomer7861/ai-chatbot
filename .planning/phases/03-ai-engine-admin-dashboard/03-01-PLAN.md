# Plan: 03-01 - Gemini Integration & RAG Logic

**Wave**: 1
**Depends on**: Phase 2
**Files modified**: package.json, app/routes/api.chat.tsx, app/models/ai.server.ts
**Autonomous**: true

## Goal
Connect the Gemini AI brain to the chat endpoint and implement the RAG prompt logic.

## Tasks

<task>
<read_first>
- package.json
</read_first>
<action>
Install the Google AI SDK:
`npm install @google/generative-ai`
</action>
<acceptance_criteria>
- `@google/generative-ai` is listed in `dependencies`.
</acceptance_criteria>
</task>

<task>
<read_first>
- .env
- .planning/phases/03-ai-engine-admin-dashboard/01-RESEARCH.md
</read_first>
<action>
Implement `app/models/ai.server.ts` to handle Gemini initialization and prompt generation.
Include the system persona and context injection logic.
</action>
<acceptance_criteria>
- `ai.server.ts` exports a function (e.g., `generateResponse`) that takes context and a prompt.
</acceptance_criteria>
</task>

<task>
<read_first>
- app/routes/api.chat.tsx
</read_first>
<action>
Update the chat endpoint to:
1. Retrieve knowledge context (done in Phase 2).
2. Pass context + user message to `generateResponse`.
3. Return the AI-generated answer to the storefront.
</action>
<acceptance_criteria>
- `/api/chat` returns a `response` field containing the AI's answer.
</acceptance_criteria>
</task>

## Verification
- Use `curl` or Postman to send a message to `/api/chat` and verify it returns a coherent, grounded answer based on your products.
- Verify that questions about non-existent products are handled according to the system prompt ("I don't have that information").

## Must Haves
- Gemini SDK installed.
- RAG prompt logic implemented.
- Chat endpoint returning AI responses.
